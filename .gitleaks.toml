[allowlist]
  description = "Allowlist for known non-secret values"
  # manifest.json contains the Chrome extension public signing key (MIIBIjAN…).
  # This is a public RSA key required by the Chrome Web Store, not a secret.
  paths = ["manifest\\.json"]
