# OSV Scanner allowlist — consumed by the `osv` job in .github/workflows/security.yml
# via `--config=osv-scanner.toml`.
#
# No active suppressions. History of forcing-function advisories that lived here:
# - OPS-2: vite GHSA-4w7w-66w2-5vf9, esbuild GHSA-67mh-4wv8-2f99, ws
#   GHSA-58qx-3vcg-4xpx — genuinely fixed by vite 5 -> 6.4.2 + ws 8.20.0 -> 8.21.0.
# - OPS-5: vitest GHSA-5xrq-8626-4rwp (CVSS 9.8, dev-only) — briefly time-boxed
#   2026-06-01 (#244) to unblock the required OSV gate, now GENUINELY FIXED (not
#   suppressed) by the vitest 3.2.4 -> 4.1.8 upgrade in this same PR. Time-box
#   removed.
#
# The file is kept (with the `--config` flag) as the home + documented pattern
# for any future time-boxed exception: an [[IgnoredVulns]] block with an
# ignoreUntil + reason.
